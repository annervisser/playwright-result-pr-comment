name: 'Playwright results as PR comment'
description: 'Take the json output of playwright and use it to create a nicely formatted PR comment'
inputs:
  playwright-json-path:  # id of input
    description: 'Path to a json file generated by playwright'
    required: true
runs:
  using: 'node16'
  main: 'index.js'
