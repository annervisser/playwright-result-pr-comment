on: [ push ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Hello world action step
        id: hello
        uses: ./
        with:
          playwright-json-path: './.github/example-report.json'
